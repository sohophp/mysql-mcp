# Development environment variables for mysql-mcp
# Copy to .env.dev and fill with real values to run the server locally
MYSQL_HOST=127.0.0.1
MYSQL_USER=your_user
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=your_database

